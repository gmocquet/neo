[project]
name = "repo-settings"
version = "0.0.0"
description = "Export and apply the GitHub repository configuration as code."
requires-python = "==3.14.6"
dependencies = ["jsonschema==4.26.0"]

[tool.uv]
package = false
